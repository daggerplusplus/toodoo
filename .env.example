# Path to the SQLite database file.
# In Docker, set this to a path inside a mounted volume (e.g. /data/todo.db).
# Defaults to todo.db in the app directory if not set.
TODO_DB=

# Host and port for the web server.
# Defaults: HOST=0.0.0.0, PORT=8001
HOST=
PORT=
